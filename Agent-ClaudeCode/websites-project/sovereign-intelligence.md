---
name: sovereign-intelligence
description: Feerasta Sovereign buildout plan + Intelligence plug-and-play agent templates (receptionist/operator/textback/responder)
metadata: 
  node_type: memory
  type: project
  originSessionId: 0b7d2d46-71b4-435e-9585-5da67e42f593
---

Built out the top two Feerasta tiers 2026-06-10. See [[website-business]], [[tenant-agents-layer]], [[voice-agent]], [[pricing-two-market]].

**Feerasta Intelligence = AI employees, productized as 8 plug-and-play templates** in `docs/intelligence/` (one shared `client-config.template.yaml` is the only per-client artifact; each template = parameterized system prompt + flow + runtime + compliance + indicative price):
1. **AI Receptionist** (inbound voice, ElevenLabs+Twilio) — flagship, near-universal.
2. **Outbound Operator** ("call operator", ElevenLabs outbound) — reminders/callbacks/review calls.
3. **Missed-Call Text-Back** (Twilio mctb-worker+Hermes) — cheapest/highest-ROI; **A2P 10DLC gate** before live SMS.
4. **Lead Responder / Front Desk** (Hermes tenant agent + WhatsApp + Resend + web form).
5. **Appointment Scheduler & Reminders** (voice+SMS+calendar Google/Outlook/Calendly/Acuity; no-show recovery + waitlist) — added 2026-06-11.
6. **Review & Reputation Builder** (SMS+email; happy→Google link, unhappy→private owner feedback, honest only) — added 2026-06-11.
7. **Database Reactivation** (SMS+email win-back of past customers from Supabase growth; CASL/A2P/CAN-SPAM) — added 2026-06-11.
8. **FAQ / Knowledge Concierge** (web chat + voice deflection from a KB off the site+Google profile) — added 2026-06-11.
Templates 05-08 added 2026-06-11 from 2026 research on top local-business AI-agent use cases (receptionist/scheduling/missed-call/speed-to-lead/reviews/reactivation). **Sales artifact: `docs/intelligence/windsor-detroit-menu.md`** = by-industry field menu (12 Windsor/Detroit business types × lead-with agent × add-ons × why × pricing) + the owner pitch frame ("AI employee vs $3-4k/mo human, 6-in-10 calls missed, 78% go with first responder") + the cross-border CASL(Windsor)/TCPA+A2P(Detroit) notes. PDF: `~/Downloads/Claude Files/Feerasta-Intelligence-Windsor-Detroit-Menu.pdf`. Default opener still Receptionist(01)+Text-Back(03); Scheduler(05) = 3rd leg for appointment shops = the "Front Desk" bundle ($1.5-2.8k setup + $700-1,200/mo).
Runtimes already exist: voice-agent/ (ElevenLabs), mctb-worker/ (Twilio), archie-tenant-agents/ (multi-tenant chat), lead-engine reviews. Indicative pricing in README (receptionist setup $750-1.5k + $300-600/mo etc.), scoped per client, no public price. Deploying a live agent = paid ElevenLabs/Twilio (ask first); the templates themselves are free spec.

**Feerasta Sovereign** = client's own private AI stack, plan in `docs/feerasta-sovereign-buildout.md`: paid **Sovereignty Assessment** ($2.5-7.5k, creditable, doubles as qualifier — most "Sovereign" prospects actually need Intelligence) with a 12-Q scored instrument; 3 deployment options (A self-hosted open models Llama/Qwen/Mistral via Ollama→vLLM, B VPC frontier Azure OpenAI/Bedrock in client tenant = common mid-market fit, C hybrid by data sensitivity); 3 tiers S1 Private Knowledge Brain / S2 Sovereign Agent Platform / S3 Full Private AI Stack (build $10k-150k+, $1.5k-20k+/mo, GPU/cloud at cost in client's account); sell-before-build roadmap, Phase 1 = dogfood a self-hosted reference brain (Ollama+Qwen+pgvector+Hermes) that doubles as the demo+clone template.

**Phase 1 reference brain LIVE (2026-06-10):** Ollama installed on **milo** (Jetson Orin, JetPack6 GPU build, ollama 0.30.7) as a systemd service (auto-start), running **qwen2.5:3b 100% on GPU**, OpenAI-compatible API at **http://localhost:11434/v1**, bound localhost-only (nothing leaves the box — the purest sovereignty demo). Done locally by milo's own agent (the remote-ssh-from-Mac route kept dropping on the 1.5GB download — lesson: hand long installs to the box's own agent). NOT on hermes (prod Archie host, load ~7/8 — would degrade Archie). To go bigger: qwen2.5:7b if RAM allows. To reach from Mac/tailnet: OLLAMA_HOST=0.0.0.0 (but breaks "nothing leaves the box"). **S1 PRIVATE KNOWLEDGE BRAIN WORKING (2026-06-10):** Built `~/sovereign-brain/` on milo: `rag.py` (stdlib-only, no pip — robust over milo's flaky ssh) does private RAG = embeddings via Ollama **nomic-embed-text** + generation via **qwen2.5:3b** (GPU), cosine retrieval, cited answers. Corpus in `~/sovereign-brain/docs/` (sample Feerasta tier/pricing facts). Verified: `python3 rag.py ingest` then `ask "..."` → answered the $2,500-7,500 Assessment fee + S1/S2/S3 tiers correctly FROM the ingested docs (not model training) with [source] citations, on-GPU, localhost-only. This IS the demoable S1 reference. Transfer files to milo via `tar -C local -cf - . | ssh jetson 'tar -C ~/sovereign-brain -xf -'` (file-transfer-over-ssh works; long downloads/detached jobs over ssh are flaky → let milo's own agent do those). **Next:** swap json store → pgvector for production; point Hermes at localhost:11434/v1; ingest a real client corpus per engagement (clone-per-client = the S1 product).

**LAW-FIRM vertical built (2026-06-10):** `rag.py` now PROFILE-based (`rag.py <profile> ingest|ask`; profiles/<name>/{docs,prompt.txt,store.json} = clone-per-client). Built `profiles/law-demo/` (fictional Hudson & Vance LLP corpus + a legal guardrail prompt: answer only from firm docs, cite source file, refuse with "I don't find that in the firm's records", NEVER invent a case/citation/clause, privileged, not legal advice). Maps the real cloud-AI blockers (ABA Formal Opinion 512 Jul 2024 + Rule 1.6: confidentiality, self-learning-tool consent, hallucinated citations/Mata-v-Avianca sanctions, OCG/data-residency, competence/supervision) → solved by on-prem + RAG-grounding + refusal. Verified live on milo: confidentiality-policy Q (cited), private matter-memo Q (cited), and the anti-hallucination test (refused a fake "Smith v. Jones" instead of inventing a holding). Writeup: `docs/sovereign-law-firms.md`. Law firms = strongest Sovereign vertical (ethics FORCES private AI).

**S1 LAW BRAIN PRODUCTIZED (2026-06-10, later):** Did the 4 buildout items via sub-agents. (1) **Richer corpus:** swapped the 5-doc sample for a 9-doc internally-consistent Hudson & Vance LLP corpus (firm-ai-policy, confidentiality-privilege, engagement-terms w/ 2026 rate card $825/$550/$425/$225, clause-bank, 3 matter memos = 2031 Brightwater supply-contract / 2044 Orchard constructive-eviction / 2050 Vantage trade-secret+non-compete, litigation-hold-memo, intake-conflicts-checklist; attorneys Hudson/Vance/Raman/Cho consistent throughout). Re-ingested on milo (9 chunks). (2) **Staff web chat UI LIVE:** `serve.py` (stdlib only) on milo at `127.0.0.1:8800`, localhost-only, branded navy/gold, `POST /api/ask {profile,question}` + `/api/profiles` + `/health`; reuses the same profiles/store/prompt as the CLI so web == CLI. Launched detached via `/tmp/start_serve.sh` (setsid; NOT yet a systemd service — to make permanent, add a user unit like the ollama one). **Bug found+fixed:** serve.py's store loader missed milo's `emb` key (milo rag.py writes `{source,text,emb}`) → patched the `or`-chain; and its prompt wrapper (`[1] (source:x)` + "if not enough say so") over-refused → mirrored rag.py's `[filename] text` + bare CONTEXT/QUESTION frame, no temp override. (3) **Refusal prompt rebalanced:** old strict version over-refused (the ChatGPT-policy Q came back as a false "I don't find that" even though firm-ai-policy.txt was retrieved #1). New balanced prompt.txt answers when docs cover the topic, else returns exactly "I don't find that in the firm's records." with NO bracketed filename. Verified 4/4 (policy/rate/matter/fake-case) on BOTH CLI and web. (4) **pgvector staged** on milo `~/legal-rag-pg/` (schema.sql+rag_pg.py+INSTALL.md) — Postgres16+pgvector install still TODO, hand to milo's own agent (long apt build, ssh drops on long ops). Repo now has full clone-per-client kit: `sovereign-brain/{rag.py,serve.py,profiles/law-demo/{docs,prompt.txt}}` + `legal-rag-pg/`. **PENDING M DECISION:** wiring Hermes onto the brain conflicts with localhost-only ("nothing leaves the box") — needs exposing milo over tailnet (`OLLAMA_HOST=0.0.0.0` or serve on the tailscale IP), ask before doing. **milo ssh remains flaky on long ops** — pattern that works: write script local → push via `cat>` foreground → launch with `setsid ... </dev/null` → poll a result file with a `tail`-based until-loop (milo has no `rg`, and the local grep-hook blocks the word `grep` even in remote ssh strings). Positioning: Intelligence="AI employee on our shared stack", Sovereign="your own AI on your infra you keep even if we walk away" — Private. Owned. Yours. Compliance kept honest ("HIPAA-aware/SOC2-aligned", never "certified"). sovereign.html already exists → just attach the Assessment offer. **DONE 2026-06-11:** sovereign.html now shows the Assessment priced ("From $2,500 · fixed fee, fully credited") + a new "For law firms" section (cited answers from firm matters / refuses to invent authority / privileged stays privileged / live-demo-on-our-own-hardware proof line + confidential-demo CTA); agents.json sovereign pricing updated to name the assessment fee. Deployed live + verified (version a52b2753). Note: piping curl JSON straight into python json.load gave a bogus control-char error; download to file first.
