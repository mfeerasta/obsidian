---
name: archie-website-funnel
description: The website business deployed as an autonomous Archie skill on hermes
metadata: 
  node_type: memory
  type: project
  originSessionId: 0b7d2d46-71b4-435e-9585-5da67e42f593
---

The Windsor/Detroit website business (see [[website-business]]) is deployed as an autonomous skill on **Archie** (M's personal agent OS on the hermes VPS, /home/hermes/.hermes, root@hermes / 100.124.152.10 / 46.225.157.75).

**Archie = hermes-agent engine + `archie` CLI front door.** Skills in ~/.hermes/skills/<name>/SKILL.md. Cron jobs in ~/.hermes/cron/jobs.json run scripts from ~/.hermes/scripts/ (archie-*.sh wrappers). Human-in-loop via `approval-gates` skill (ed25519-signed, WhatsApp "approve <id>"). Comms: WhatsApp via `archie whatsapp send`, Gmail drafts (hard rule: NEVER auto-send Gmail; money moves go through approval-gates). Archie's Supabase (SUPABASE_DB_URL_RW) is the SAME project as the website CRM (qcvxefbrzkspoldjydrx), so the `leads` schema is shared — no migration.

**Deployed skill `website-funnel`** (python, ports the mac lead-engine):
- Runtime: /home/hermes/.hermes/skills/website-funnel/ (common.py + leadgen/audit/draft/bill/run.py). Canonical copy in mac ~/.claude/skills/website-funnel/ (sync source) + dev copy in repo archie-website-funnel/.
- common.py: asyncpg direct to leads schema, WhatsApp + approvals helpers. Verified working on hermes (draft --print-only pulled real prospect).
- Cron wrapper ~/.hermes/scripts/archie-website-funnel.sh; job `archie-website-funnel` (id 9433642914b6) registered PAUSED at cron `30 4 * * *` (09:30 LHE).
- Autonomy (M's choice): daily run.py = leadgen->audit->draft->WhatsApp brief. M reviews+sends drafts. Billing via approval-gates (M approves on WhatsApp). No auto-send, no ungated charges.
- **Sender = feerasta.ai mailboxes** (rebranded from Feerstone 2026-06-08; NOT personal Gmail). **Real mailbox `meer@feerasta.ai`** (pass `Meer1005-`, Hostinger smtp/imap.hostinger.com, SMTP :587 STARTTLS verified) is the SMTP/IMAP **auth login**; each division sends **From its own send-as alias** (Studio → `studio@feerasta.ai`, Ascent → `ascent@`, Sovereign → `sovereign@`, Enterprise → `enterprise@`, Intelligence → `intelligence@`/`billing@`, general → `contact@`, agent → `archie@`). Aliases are Hostinger forward-only → meer@feerasta.ai; **M creates them in hPanel** (Hostinger MCP can't make mailboxes/aliases). Display name **"M. Feerasta"**. hermes .env keys renamed `FEERSTONE_*` → `FEERASTA_*` (FROM_EMAIL=studio@feerasta.ai, SMTP_USER=meer@feerasta.ai); common.py dict `FEERASTA`, Message-ID domain feerasta.ai. .env backup `.env.bak-pre-feerasta`. Deliverability live on feerasta.ai (MX/SPF/DKIM/DMARC p=none → tighten to p=quarantine after warming). draft.py saves to meer@feerasta.ai INBOX.Drafts; M reviews+sends. **Orphans Aria/Lodestar must NOT use @feerasta.ai** (anonymity) — own domains.
- **feerstone.com RETIRED** (M decision 2026-06-08, no 301). **Mac repo rebrand DONE + pushed** (2 commits on main): Tier A `4564299` (46 files: lead-engine src, docs, portfolio studio.html, prospect-video, voice templates; demo/cal URLs → feerasta.ai; renamed docs/feerasta-ai-officer-tier2.md), Tier B `4f3671c` (mctb-worker + migration comments; stripe/setup-catalog.py prose — **idem keys `feerstone-*` + metadata KEPT** to avoid duplicating existing Stripe objects, NOTE in file; bump prefix only for a fresh catalog). feerstone-dns-backup json kept as historical.
- **Still need re-provisioning on feerasta.ai** (code points there, they 404): `demo.feerasta.ai` (demo sites host) + `cal.feerasta.ai` (Cal.com custom domain) — need M's hosting/Cal.com targets, then DNS in Cloudflare.
- **DEMO HOSTING decision DEFERRED (M: "save for down the road" 2026-06-08).** Investigated: there is **no multi-tenant demo worker** — CF worker `feerstone-demo` serves ONE baked sample site (Maple Ridge HVAC, Astro) and 403s unknown Hosts; the real comfort-tek demo ran on **Lovable** (A `185.158.133.1` + `_lovable` verify TXT on feerstone.com); separate `feerstone-demo-comfort-tek` worker exists (1-worker-per-client). So a wildcard `*.demo → feerstone-demo` is WRONG (every prospect gets the sample/403). **My long-run recommendation = build a multi-tenant Worker** serving `<slug>.demo.feerasta.ai` from R2 by Host (matches `docs/demo-build-methodology.md`): one wildcard DNS + one route, Archie uploads `dist/<slug>` to R2, near-zero marginal cost, fully automatable. Stopgap = point bare demo.feerasta.ai → feerstone-demo (sample live). CF acct `61d561ca9690ad9b59d9449181d90f12`, workers.dev subdomain `feerstone`. cal.feerasta.ai: held pending M's Cal.com link (free 301 redirect vs paid custom domain).

## Public site DEPLOYED + feerstone.com retired (2026-06-08)
- **Feerasta house site is LIVE at feerasta.ai** (+ www). Deployed as a Cloudflare **static-assets Worker `feerasta-site`** (account `61d561ca9690ad9b59d9449181d90f12`, workers.dev subdomain `feerstone`, preview `feerasta-site.feerstone.workers.dev`). Published: house `index` + 5 divisions (studio/ascent/sovereign/enterprise/intelligence) + the Intelligence demo. **Excluded** (do not deploy): orphan brands `aria/`+`lodestar/` (anonymity), internal docs (avatar-plan/qbo-prep/hermes-efficiency/claude-research/image-prompts/onepager). Reproducible: `portfolio-site/deploy.sh` (uses `CLOUDFLARE_WORKERS_API_TOKEN`, Workers Scripts:Edit). `.html` links 307→pretty URLs (`/studio` etc.), all 200.
- **Root swap (apps untouched):** deleted the apex A (`feerasta.ai`→hermes "Admin" stub) + attached `feerasta.ai`+`www` as worker custom domains (apex now AAAA `100::` proxied + worker binding). **App subdomains `sentinel/haazri/otp/manager/agri/api/polymath/...` → hermes 46.225.157.75 UNTOUCHED; mail (MX/SPF/DKIM) intact.** NB: after the swap, a stale LOCAL Mac DNS cache showed the old hermes "Admin"/nginx for ~min — public resolvers (1.1.1.1/8.8.8.8) were correct immediately; verify via `--resolve feerasta.ai:443:<CF-IP>` not the system resolver.
- **feerstone.com RETIRED:** detached all 3 worker-domain bindings (`feerstone.com`+`www`→`feerstone` worker, `demo.feerstone.com`→`feerstone-demo`). No 301 (M's choice). Old Feerstone site no longer served.
- **OPEN:** page `<title>`s contain em dashes ("Feerasta — Private AI") — violates the [[no-em-dashes]] rule; offered to swap site-wide separator + redeploy, pending M.

## DNS reality — Cloudflare authoritative, NOT Hostinger (verified 2026-06-08)
- **feerasta.ai DNS runs on Cloudflare** (NS `paityn`/`newt.ns.cloudflare.com`; CF zone `0e959a4fd92548c65126e0dd241f5836`, account "Meerfeerasta@gmail.com's Account", acct id `61d561ca9690ad9b59d9449181d90f12`). **The Hostinger MCP `DNS_*` tools read Hostinger's OWN (unused) zone for feerasta.ai → NON-AUTHORITATIVE, disregard them.** Do all feerasta.ai DNS via the **Cloudflare MCP** (`cloudflare-api execute`). Root A = 46.225.157.75 (hermes); many app subdomains (sentinel/haazri/polymath/table/etc.) → also hermes.
- **DKIM fix 2026-06-08:** the 3 Hostinger DKIM CNAMEs (`hostingermail-a/-b/-c._domainkey` → `hostingermail-{a,b,c}.dkim.mail.hostinger.com`) existed in CF but were **`proxied:true` (orange cloud) → unresolvable → DKIM broken**. Un-proxied all 3 to DNS-only (also autodiscover/autoconfig). Verified resolving on both CF NS. **Lesson: Hostinger mail CNAMEs on Cloudflare MUST be grey-cloud/DNS-only.** MX (mx1/mx2.hostinger.com) + SPF (`include:_spf.mail.hostinger.com ~all`) intact. **DMARC still `p=none`** — tighten to `p=quarantine` after ~2-4 wks warming.

**TO GO LIVE (M must run; agent was blocked from writing secrets to the shared box):** see archie-website-funnel/DEPLOY.md — (1) append GOOGLE_MAPS_API_KEY + FIRECRAWL_API_KEY to hermes ~/.hermes/.env, (2) smoke-test `run.py --dry-run --no-draft`, (3) enable the cron job (set enabled:true/state scheduled), (4) optional STRIPE_SECRET_KEY for bill.py. Sibling skill `outbound-sales-research` is the same pattern for M's separate on-prem AI business.

## Verified on hermes 2026-06-07
- **"Hermes Agent" == Archie** (M confirmed same thing).
- **Gateway IS running**: systemd `hermes-gateway.service` active ("Hermes Agent Gateway — Messaging Platform Integration"); process `/usr/local/lib/hermes-agent/venv/bin/python -m hermes_cli.main gateway run --replace` (user `hermes`, up since Jun 1) + WhatsApp bridge `scripts/whatsapp-bridge/bridge.js --port 3000 --mode self-chat` + PM2 God Daemon. Install dir **/usr/local/lib/hermes-agent**; state /home/hermes/.hermes. Runs as systemd+PM2, NOT docker/cron (no docker containers, no archie timer) — website-funnel cron still paused, consistent.
- **Access reality**: hermes SSH is firewalled on the public IP (46.225.157.75:22 filtered) — reachable ONLY over Tailscale (100.124.152.10). Tailscale.app is installed on M's Mac but was *stopped*; `open -a Tailscale` to bring it up, then `ssh hermes`. Tailscale SSH runs an extra browser auth check on connect.
- **⚠️ Security incident on hermes (~mid/late May 2026)**: SSH-key compromise, attacker IP **154.80.40.98**, attacker key prefix `LvV4Zb…`. /root home holds forensics-*.tar.zst, forensics-quarantine-systemtd, incident-evidence-20260531, malware-quarantine, sentinel.bak.*, dot-hermes-root-archive. The wedged root forensics hunt process (`rg … /`) was killed 2026-06-07 (the open question "is the attacker key still authorized anywhere" was NOT re-run — M declined). Still needs a real cleanup + key/persistence verification pass.

## Hermes platform unified + upgraded 2026-06-07 (plan: ~/.claude/plans/clever-splashing-narwhal.md)
Hermes = **Nous Research Hermes Agent** (github.com/NousResearch/hermes-agent), self-improving agent OS; install /usr/local/lib/hermes-agent (root-owned editable git). "Hermes Agent" == Archie.
- **Upgraded v0.12.0 → v2026.6.5** ("Surface Release") via `hermes update --backup` (git pull, auto-backup, config v11→v12 migration clean, 74 bundled skills synced, gateway auto-restarted). All 23 crons + custom skills + memories survived.
- **Single source of truth: `/home/hermes/.hermes`** (user `hermes`). The old `/root/.hermes` split (held 4 dead personal-finance crons) was consolidated: migrated hbl-monthly/personal-pk/cdc-charges/asif-study into the canonical profile via `hermes cron create`, then retired /root/.hermes. NB: running `hermes update` as root re-scaffolds an inert `/root/.hermes` (bundled-skill dupes + the update backup) — retire it again, or run updates with HERMES_HOME=/home/hermes/.hermes set. Update backups land in /root/.hermes/backups/*.zip.
- **Desktop ↔ server = one brain via the dashboard** (NOT ACP — ACP is local-IDE only). New systemd `hermes-dashboard.service`: `hermes dashboard --no-open --host 100.124.152.10 --port 9119 --skip-build` — bound to the **Tailscale IP only** (public 46.225 firewalled), auth-gated. **Key gotcha: `--insecure` is a legacy escape hatch that DISABLES the auth gate** (auth_required:false). For a remote desktop, configure the `dashboard_auth/basic` provider in config.yaml (`dashboard.basic_auth.username` + scrypt `password_hash` + `secret`, via `hermes config set`) and bind non-loopback WITHOUT `--insecure` → gate engages (auth_required:true). Creds (user `meer`) in ~/Desktop/feerasta-credentials.md; stored server-side only as scrypt hash in config.yaml (0600).
- **Desktop connect:** Hermes Desktop → Settings → Gateway → Remote gateway → `http://100.124.152.10:9119` → Sign in. Reuses the server's same config/skills/memory/sessions. Do NOT let the desktop run its own standalone local agent.
- Cleaned up the vestigial PM2 daemon (managed 0 apps; WhatsApp bridge is the gateway's child). Backups in /root/backups/. Adding new business skills (Feerasta house) is the deferred next step, now that the platform is unified.

## Efficiency + MCP wiring 2026-06-07
- **MCP servers now: hostinger, stripe, supabase-crm, resend** (`hermes mcp list`). **supabase-crm** = read-only Postgres MCP (`@modelcontextprotocol/server-postgres`) → growth/leads CRM via a dedicated least-privilege role **`archie_ro`** (scoped to growth+leads only, blocked from the Guard-Patrol `tabl` schema). Connection proven via node-pg (growth=4 tables, tabl=0). Uses the **Supavisor session pooler** (`aws-1-ap-south-1.pooler.supabase.com:5432`, user `archie_ro.qcvxefbrzkspoldjydrx`) — the direct `db.<ref>` host is IPv6-only now. SSL = `verify-full` against the **pinned Supabase CA** at `/home/hermes/.postgresql/root.crt` (extracted via openssl s_client; node-pg lacks it in its bundle). archie_ro password in `/home/hermes/.hermes/.archie_ro_pw` (0600) + inline in config.yaml mcp arg. **resend** MCP wired from RESEND_API_KEY already in hermes .env. NB: `hermes mcp test` has a ~10s probe that always times out on these (npx cold-start + handshake) and `-z --cli` one-shots don't load MCP — validate via WhatsApp/desktop, not those.
- Efficiency: `tools.tool_search.enabled=auto` (defers MCP schemas), `sessions optimize` run, **MEMORY.md trimmed** 2366→1406 chars (backup MEMORY.md.bak-*), **`/morning-ops` skill bundle** created (website-funnel+local-seo-audit+invoice-chaser).

## NVIDIA fallback + aux routing 2026-06-08 (M approved, free-tier only)
- **nvidia is a first-class BUILT-IN provider** in hermes v2026.6.5 (`auth.resolve_provider("nvidia")` → `nvidia`), keyed off `NVIDIA_API_KEY` (already in hermes .env) against the **free hosted NIM API** `integrate.api.nvidia.com/v1`. The older fallback doc comment in config.yaml doesn't list it, but it's recognized. A hand-added `providers.nvidia` custom dict entry is therefore **shadowed/ignored** (kept as harmless self-doc/backup).
- **Fallback chain:** `fallback_providers: [{provider: nvidia, model: meta/llama-3.3-70b-instruct}]` (free NIM, 128k ctx). Fires only on primary (openai-codex/gpt-5.5) 429/529/503/conn-fail. Key smoke-tested live → HTTP 200.
- **Aux slots all routed to nvidia free models** (offloads the openai-codex sub): vision → `meta/llama-3.2-90b-vision-instruct` (free VLM), all other 11 slots → `meta/llama-3.3-70b-instruct`. hermes aux fallback if nvidia unavailable = `openrouter:google/gemini-3-flash`. Constraint honored: **free hosted NIM only, no paid/dedicated endpoint.** config.yaml backup: `config.yaml.bak-pre-nvidia-twilio`.
- **Twilio MCP — WIRED + live.** Now in `mcp_servers.twilio`: `npx -y @twilio-alpha/mcp <SID>/<API_KEY>:<API_SECRET>` (positional cred). `mcp test twilio` connects + enumerates the full Twilio REST tool surface (CreateMessage, lookups, usage, etc.). **`@twilio-alpha/mcp` v0.7.0 parses creds ONLY from argv** (minimist `--accountSid/--apiKey/--apiSecret` or positional `SID/KEY:SECRET`) — zero env-var support — so the secret IS visible in `ps`. **M explicitly authorized this ps-exposure trade-off 2026-06-08** (same pattern as the existing stripe MCP; Tailscale-only single-tenant box). The package also emits a malformed JSON-RPC `log` message (method+id) → benign `literal_error: input 'log'` noise in the gateway journal, does NOT break the connection. Creds also staged in hermes .env (0600: TWILIO_ACCOUNT_SID/AUTH_TOKEN/API_KEY/API_SECRET/FROM) + the REST basic-auth path is proven (Twilio API → 200, account "Sentinel"). Still: FROM +12495231208 NOT provisioned + US/CA SMS needs A2P 10DLC. Gateway also logs a pre-existing `[sms] TWILIO_PHONE_NUMBER not set` from its built-in SMS platform (separate feature, predates this).

