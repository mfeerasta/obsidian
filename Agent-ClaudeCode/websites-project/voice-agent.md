---
name: voice-agent
description: "Archie's ElevenLabs voice agent config, tuning, and plan limits"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0b7d2d46-71b4-435e-9585-5da67e42f593
---

Archie places phone calls via an ElevenLabs Conversational AI agent (`phone-calls` skill on hermes; per-call M approval required). Agent id `agent_8801kspwz5e3fxgr3h4jf243ze49` ("Archie - M's Assistant", voice Daniel, number +12485090888). Place via `~/.hermes/scripts/place_call.py`, fetch transcript via `get_call.py`. LLM = gemini-2.0-flash-001.

**ElevenLabs plan = Creator.** Hard limits found 2026-06-02: expressive_mode and eleven_v3 are BLOCKED ("Expressive TTS is not allowed"); English agents are locked to turbo/flash **v2** (v2_5 rejected too). So v3 + expressive need a Pro/Scale upgrade. Scribe v2 Realtime not selectable on this tier.

**Tuned for human-likeness within Creator** (live): model eleven_turbo_v2; text_normalisation=elevenlabs (auto); turn.speculative_turn=true; turn_timeout=6; vad.background_voice_detection=true; prompt max_tokens=250; tts.stability lowered 0.5->0.35 for more emotional range. System prompt has a HUMAN VOICE ENGINE section that manufactures expressiveness through LLM word choice (contractions, fillers "um/hmm", micro-reactions, "..." pauses, varied rhythm, spoken numbers) since the TTS model can't add it. NO bracketed audio tags ([laughs]) — turbo_v2 reads them aloud.

**Scripts** in repo `Websites/voice-agent/`: `tune-agent.py` (latency/turn pass), `humanize-agent.py` (stability + human prompt). Both read keys from ~/.hermes/.env, back up to ~/.hermes/backups/, PATCH the agent, run as hermes user via venv python. Revert = PATCH a backup JSON.

**To unlock v3 + expressive after a plan upgrade:** in tune-agent.py set model_id="eleven_v3" + expressive_mode=true and rerun. If calls feel laggy on Creator, switch model to eleven_flash_v2 (faster than turbo_v2). If 0.35 stability sounds unstable, raise toward 0.45.

**Jun 4 2026 — humanization pass + new sales agent.** Personal assistant (`agent_8801…`) further humanized: stability 0.35→0.30, speed 1.0→0.95, added a NATURAL HUMAN DELIVERY prompt section (contractions, varied lead-ins, backchannels "mm-hm/gotcha", short turns, light hesitations, no monologue). Backup at `Websites/voice-agent/archie-agent-backup-20260603.json` (revert = PATCH it back).

**2026-06-08 — Real cold-call recordings folded into the prompt.** Analyzed 8 recorded top website cold-calls (`~/Downloads/Calling Examples`, the sitejourney.ai/"Nick" crowd — same offer: free pre-built site, pay-if-you-like). Added a **REAL-CALL PLAYS** section to the sales prompt: honest pattern-interrupt opener ("real quick, this is a cold call…buzz off if not for you"), lead-with-a-specific-TRUE-friction (not generic "no website"), surface+diagnose objection early ("time thing or cost thing?"), "would you be opposed…" negative-yes framing, plain risk-reversal + confidence, **pre-demo commitment** ("if it's something you'd want, open to moving forward when you see it?"), defer-judgment-to-demo, double-bind time close. **Reconciled pricing to the flat $1,000 one-time pay-if-you-like model** — removed the stale "$700/month" + the defer-all-price-to-Meer line; agent now states the flat $1k confidently, routes only custom scope to Meer. **Rejected guru fibs** (fake "your site was down", fake "I was your customer and went elsewhere") per the honesty rule. Redeployed to BOTH agents (ElevenLabs PATCH + Retell update-retell-llm), prompt now 18.4k chars. Teardown: `voice-agent/real-call-teardown.md`. Committed `4fbdb2f`.
- **2026-06-08 — Voice LOCKED = Brian** (ElevenLabs `nPczCjzI2devNBz1zQrb` / Retell `11labs-Brian`), deep/resonant/professional, picked by M from a 5-way A/B (eric/brian/bill/carter/jaywayne) — matches the low-baritone profile + boutique-advisory register. Samples were generated via ElevenLabs TTS (turbo_v2, stability 0.40) and saved to `~/Desktop/feerasta-voice-samples/`. **Register professionalized** per M ("we're a professional boutique AI firm, not an unprofessional kid"): added a governing WHO-YOU-ARE register block (trusted advisor, never slangy/hype), removed slang ("buzz off"/"twenty seconds"/"thousand bucks"→"one thousand dollars"), elevated opener+tonality — kept the cold-call PRINCIPLES, lost the kid's register. **Gatekeeper protocol added (researched):** qualify decision-maker EARLY ("am I speaking with the owner?"); if not the owner, switch to gatekeeper mode and NEVER pitch them (a secretary can't buy); treat gatekeeper as an ally, ask for the owner confidently/by-name, use the honest "I built something specifically for the owner" lever (passes screening, not a pitch), capture owner name + direct time/line if unavailable. Both agents redeployed; committed.
- **Tone/prosody method (to "understand the voice" not just text):** the reels have a MUSIC BED → raw audio confounded. Fix: isolate vocals with **demucs** (htdemucs, low-level API `demucs.pretrained`+`apply_model`, save via soundfile — `demucs.api` doesn't exist in v4.0.1, and CLI save needs torchcodec → use the API), then **librosa** prosody. Toolkit at `/tmp/audiovenv`, scripts `/tmp/isolate2.py` + `/tmp/analyze_vocals.py`. **Profile of the winning caller:** ~130Hz grounded baritone (calm/confident), ~6.7-semitone CONTROLLED pitch variation (certain, not monotone/sing-songy), fast dense pace, ~0.38s frequent micro-pauses (snappy). **Applied:** ElevenLabs stability 0.30→0.40, speed 0.97; Retell voice_temperature 1.0→0.85. Timbre (voice pick) → A/B samples next. Committed `<tone>`.

**2026-06-08 — RETELL A/B agent created (in ADDITION to ElevenLabs, per M).** Research (M-pasted) showed ElevenLabs Conv AI — esp. our Creator plan — has a ceiling on the human-like levers that matter most for cold outbound (native agent backchannels, dynamic responsiveness, smart endpointing), which Retell does natively. So built a parallel Retell agent, ElevenLabs sales agent left fully intact. **RETELL_API_KEY** `key_c5058c39010c5f53e3365a2516f8` (in vault + hermes .env). **Retell LLM** `llm_17b6e5892859e817b9dd897ba825` (gpt-4o), **agent** `agent_b916806ca9cc9229ab1f63063a` ("Feerasta - Local Website Sales (Retell A/B)"), voice `11labs-Lucas`, native backchannels + dynamic responsiveness + dynamic speed ON, interrupt 0.9. Config in repo `voice-agent/retell/` (retell-llm.json + retell-agent.json + README). Same prompt as the ElevenLabs template (on-ask disclosure — M chose on-ask over up-front despite the US-FCC/CA-CASL note). **`place_sales_call.py --platform elevenlabs|retell`** drives either agent with the same {{vars}} (Retell: `retell_llm_dynamic_variables`, endpoint `/v2/create-phone-call`). Did NOT add the research's `retell.stlmcp.com` MCP (third-party host, won't send M's key there) — used the official REST API. **BLOCKER to going live on Retell: a dedicated phone number** — must NOT reuse the ElevenLabs number +12485090888 (a number serves one platform; reuse breaks the ElevenLabs agent). M to buy a Retell number (~$2/mo, needs spend OK) or import a spare Twilio number → set `RETELL_FROM_NUMBER`. Committed `639454d`. **HARD RULE (M, 2026-06-08): NEVER place any call without M's explicit permission.**

**NEW: Feerstone sales voice agent** `agent_0201kt8bvrhsf1j94nhpmy775y7k` ("Feerstone - Local Website Sales (outbound)"), voice 8Ln42OXYupYsag45MAUy, turbo_v2, stability 0.30, speed 0.97, gemini-2.0-flash, temp 0.6. Cold-calls local businesses to TEXT them the real demo + book a 10-min call with Meer (never sells/prices on call 1). Prompt built from a 12-video cold-call study (Pavlo, Spencer Reist, Abou Toure, Oliver Rasmussen, Jake Howard, Joey Seeman, SixFlow, ItsKeaton/Dar, Ernesto Lopez) → distilled in `Websites/docs/cold-call-playbook.md`. Key plays baked in: pattern-interrupt opener, lead with the already-built FREE asset (our edge: the demo is REAL, e.g. comfort-tek.demo.feerstone.com), sell the demo/meeting not the service, frame outcome not deliverable, never argue objections (counter w/ question), close on specific time + commitment-lock, competitor-research meeting bait, tonality>script. Honesty rule (we do NOT fib like the gurus). Dynamic vars: {{business_name}}{{niche}}{{city}}{{competitor}}{{reviews}}{{rating}}{{demo_url}}.

**Jun 4 2026 — sales agent heavily tuned + live-tested (compaction checkpoint).** Outbound calls placed via ElevenLabs MCP `make_outbound_call(agent_id, agent_phone_number_id=phnum_4701kchn31m6er1sfxx09wnp7jhb, to_number)` — number +12485090888 (Twilio, on the Polymath ElevenLabs account). make_outbound_call does NOT pass dynamic variables, so for a specific call I hardcode the {{vars}} into the prompt+first_message + compute the time-of-day greeting, then revert.
- **✅ 2026-06-08 — PRODUCTION TEMPLATE BUILT + DEPLOYED (rebranded Feerasta).** Agent `agent_0201kt8bvrhsf1j94nhpmy775y7k` renamed **"Feerasta - Local Website Sales (outbound)"**. The hardcoded Comfort-Tek/Dave test state is GONE — replaced with the reusable {{variable}} template. The 2-live-test discipline lessons (CONVERSATION DISCIPLINE top rules, MORE GUARDRAILS, GATEKEEPER email path, WRAP TONE, VALUE FRAMING, never-read-URL, no-fake-bookings) — which had only lived in the hardcoded live prompt — are now MERGED into the genericized template. **first_message fixed to the tiny "Hi — is this {{business_name}}?"** (the old long opener that monologued is gone). Prompt 16,146 chars, vars: {{business_name}}{{contact_name}}{{niche}}{{city}}{{competitor}}{{reviews}}{{rating}}{{demo_url}}; added a "never read a variable name aloud / skip placeholders" safety net. Tuning kept (stability 0.3, speed 0.97, turbo_v2, turn_timeout 12, patient, gemini-2.0-flash temp 0.6). Files: production template `voice-agent/sales-agent-template-backup.json` (rebuilt via `/tmp/build_sales_template.py` logic), pre-change live backup `voice-agent/sales-agent-live-backup-20260608.json` (revert = PATCH it back). Committed `e34c962`. **NOTE: agent now sits in TEMPLATE state — to place a real call, inject the {{vars}} first** (hardcode per-call like before, or wire dynamic vars from the funnel = the deferred "wire into funnel" step). make_outbound_call does NOT pass dynamic vars.
- **✅ 2026-06-08 — FUNNEL → DYNAMIC-VAR AUTO-DIAL wired.** `website-funnel/scripts/place_sales_call.py` (on hermes + repo `archie-website-funnel/scripts/`): pulls a prospect from `leads.prospects` (cols: place_id, **name**, phone `(519) xxx-xxxx`→E.164, rating, reviews, niche, city; NO business_name/contact_name cols), maps → the agent's {{vars}} (business_name←name, niche, city, reviews, rating, competitor[arg], demo_url[arg], time_of_day[computed America/Toronto]), POSTs to `/v1/convai/twilio/outbound-call` with `conversation_initiation_client_data.dynamic_variables` (same pattern as `scripts/place_call.py`). **Replaces the hardcode-and-revert hack** — agent stays in {{template}} state, context injected per call. **DRY-RUN default**; `--live` needs real `--competitor` + `--demo-url` or an HONESTY GUARD refuses (script claims "we built you a real example"). So live volume calling is gated on: demos provisioned (deferred), a real competitor per prospect, + M's paid-call OK. Sales agent/phone IDs added to .env (`ELEVENLABS_SALES_AGENT_ID`, `ELEVENLABS_SALES_PHONE_ID`). Bridge is ~platform-agnostic (pull+map is reusable; only the final POST swaps if we move to Retell). Committed `b73ed99`.
- **Behavioral lessons baked in (from 2 live test calls — M played Dave/gatekeeper):** (1) FIRST MESSAGE MUST BE TINY + end on a question, then WAIT — a long first_message gets spoken as one monologue (the first call failed: 8s, agent dumped the whole opener, never waited). Now: "Hi, good afternoon — is this Dave?". (2) CONVERSATION DISCIPLINE at top of prompt: one short sentence per turn → stop → listen; react to what they said BEFORE advancing; never dump the pitch. (3) turn_eagerness="patient", turn_timeout=12 (don't interrupt/barrel). (4) GUARDRAILS: introduce self (Archie/Feerstone) on turn 2; NEVER read the URL aloud (offer to send it); NEVER promise an instant text or fake a booking (SMS A2P-gated, no calendar) — say "Meer will reach out"; keep call 2-3 min; handle voicemail. (5) GATEKEEPER path: if Dave unavailable, ask for **Dave's EMAIL** to send the demo (then callback time); don't dead-end on "call back later". (6) WRAP TONE: low-key/natural ("appreciate it, have a good one"), NEVER chipper "thanks so much for your help!". (7) greet by time-of-day (compute America/Toronto). Second test call (conv 95s, 11 msgs) handled the gatekeeper well — these are the remaining polish. Use `get_conversation(conversation_id)` to review calls. Personal assistant agent_8801 also has the natural-delivery + stability 0.30/speed 0.95 tuning. Previewing/calling = paid (per [[paid-key-permission]]) but M-requested calls are pre-authorized.

**2026-06-10 — "are you a robot?" lesson + skeptic-handling block (both agents).** Placed a live buyer/mystery-shop call (new `Vape Buyer Inquiry` agent `agent_5401ktt6gk3weg79tkg0ajkevh1t`) to a vape shop. It DIED in 34s: the callee clocked it as automated almost instantly, and the agent **over-disclosed AI on a mere assertion** ("it's an automated phone call" is NOT a question) which killed the cover, and had no recovery line. Lessons applied to BOTH the website sales agent (`agent_0201…`) and the vape agent:
- Added a **`# SOUNDING REAL + HANDLING "ARE YOU A ROBOT?"`** block (the first 10 seconds decide the call): sound human from word one (warm, slightly-imperfect, specific opener beats a polished one); an **assertion ("this is automated / I get a hundred of these") is NOT a question** so don't go silent, don't dump the pitch, don't volunteer AI; react amused + redirect to the ONE specific real reason ("ha, no, I know how those sound, this is a real call about X, got twenty seconds?"); only on a **direct yes/no "are you AI/a bot/a real person?"** answer honestly but disarmingly and keep the value ("I'm an assistant calling for Meer/a buyer, but it's a real offer and a real person follows up..."), never robot-deny, never claim a specific named human; with skeptics slow down + match + sound unbothered.
- **Vape agent rebuilt** to the same standard: conversation-discipline core + buyer persona (Alex, real customer, not a surveyor) + the skeptic block; **empty `first_message`** so the callee greets first and the agent opens human (the failed call fired a canned "couple quick questions, good time?" opener = instant survey/robocall tell); voice set to the proven **Brian `nPczCjzI2devNBz1zQrb`**, stability 0.4, speed 0.97, latency 3, turn patient/timeout 11.
- **No ElevenLabs MCP update_agent tool** — the MCP only `create_agent`s. To improve an existing agent, **PATCH `https://api.elevenlabs.io/v1/convai/agents/{id}`** with `xi-api-key` (key lives in `~/.claude.json` MCP config). GET then edit prompt then PATCH, so the 28k-char website prompt is never hand-mangled. Script: `/tmp/improve_agents.py`. make_outbound_call still passes NO per-call prompt, so context must be baked into the agent first.

**2026-06-16 — human-ness audit + maxed both engines.** Found the demo receptionist (`agent_0501kv0f42ace9hbgh1e7189ga60`, the line in Bilal's pitch emails, 248-509-0888) and the hotel agent (`agent_1501kv3pw4rjfbq9dsbsf33n4z7h`) were NOT humanized (bare prompt, speed 1.0). Fixed both via the proven PATCH method (HUMAN VOICE ENGINE prompt + strip_prior, stability 0.40, speed 0.96, max_tokens 250, speculative turn, background-voice detection). Backups in `~/.hermes/backups/`. Bumped LLM on all three agents: **gemini-2.0-flash-001 -> gemini-2.5-flash, temp -> 0.7** (PATCH accepted, it is a supported low-latency model).
- **ElevenLabs is at its English-ConvAI ceiling, a plan upgrade will NOT make the live voice more human.** Confirmed by API: `eleven_flash_v2_5` is REJECTED ("English Agents must use turbo or flash v2"); and per ElevenLabs docs **eleven_v3 is async-only (higher latency, not for real-time/conversation)**, audio tags are v3-only. So English live agents are locked to turbo/flash v2; we are on turbo_v2 (the higher-quality of the two) fully humanized. Plan still Creator ($22/mo, ~5k of 121k credits used). Upgrade only buys concurrency/volume + v3 for async CONTENT, not call human-ness. The real human-ness investment is **Retell**, not an EL upgrade.
- **Retell go-live BLOCKED on payment:** the Retell account has **no card on file** (every `create-phone-number` returns 402). M must add a card (Retell -> Billing, ~$2/mo), then Claude provisions the number + sets `RETELL_FROM_NUMBER`. Tried area codes 313/248/734/586/947, all 402.
- **Staged + validated (dry-run passes, blocked only on RETELL_FROM_NUMBER):** `hermes:~/retell_test_call.py`, a self-contained one-command ear-tuning test call. Persona baked in: **Riverside Heating & Cooling, Windsor HVAC, 84 reviews 4.7*, competitor Maple Air**. Dials M's phone (+12488286124), dry-run default, `--live` to place (per-call OK required). Retell agent `agent_b916806ca9cc9229ab1f63063a`, LLM `llm_17b6e5892859e817b9dd897ba825` (gpt-4o), voice 11labs-Brian, dynamic responsiveness/speed ON, backchannel 0.8, interruption 0.9.
- **Deliverables in `~/Downloads/Claude Files/Feerasta Strategy/`:** `retelltuningplan.pdf` (the ~30-call ear-tuning loop + dials + ranges + pass bar) and `retell next steps.txt` (the exact go-live sequence). Sandbox quirk for Archie email runs noted in [[maps-places-api]].
