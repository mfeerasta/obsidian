---
name: website-business
description: "M's local-website business for Windsor ON + Detroit MI, its model, beachhead, and stack"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0b7d2d46-71b4-435e-9585-5da67e42f593
---

M is building a productized local-visibility business: getting Windsor (Ontario) and Detroit (Michigan) local businesses found on Google and converting searches into calls. Sells outcomes (Google Map Pack visibility + lead capture), not "websites".

**Model:** productized, automate-first, solo. Near-zero spend at first, using owned infra (Hetzner VPS 46.225.157.75, Cloudflare, Supabase, Stripe, Resend, Google Places free credit).

**Beachhead:** Windsor home-service trades (HVAC first) for fast, low-competition local-SEO wins and case studies. Expand later to Detroit dental/legal (high-ticket + rank-and-rent).

**Pricing:** trades build 1.5k-2.5k + care 150-300/mo + growth 500-1.5k/mo; high-ticket build 3.5k-7.5k + retainer 1.5k-4k/mo; rank-and-rent 500-2k/mo owned asset. CAD for Windsor, USD for Detroit.

**Stack:** AI first draft then harden to crawlable Astro/Next (never ship client-side-only AI sites; Google can't crawl them). WordPress on VPS for rank-and-rent. Client always owns their domain (except rank-and-rent generic domains).

**Workspace:** /Users/meerfeerasta/Desktop/AI Projects/Websites. Plan in windsor-detroit-gameplan.md; sales assets in docs/. lead-engine/ (bun): leadgen.ts (Google Places find/score/CSV), vault.ts (SQLite CRM query), audit.ts (Firecrawl free-audit generator), sync.ts (push vault to Supabase). site-template/ (Astro, crawlable, LocalBusiness schema, one-config-file client builds, deploy to Cloudflare Pages, builds clean). Supabase leads CRM = `leads` schema in Guard Patrol project (qcvxefbrzkspoldjydrx, url https://qcvxefbrzkspoldjydrx.supabase.co): tables prospects/clients/deals, RLS on, service_role only. lead-engine scripts (all bun, typecheck clean): leadgen, vault, audit, sync, outreach (Resend, personalized, auto-picks real competitor from vault, logs status), bill (Stripe payment link + subscription -> leads.clients/deals -> prospect won), pipeline (CRM dashboard). Supabase access via SECURITY DEFINER RPCs (service_role only, no schema exposure): leads_upsert_prospects, leads_upsert_client, leads_insert_deal, leads_set_prospect_status, leads_pipeline. Keys live in lead-engine/.env (gitignored): Google Places, Firecrawl, Supabase service_role. Validated live end-to-end: 20 Windsor HVAC prospects in Supabase, pipeline + outreach draft run clean. STILL NEEDED: STRIPE_SECRET_KEY (for bill), RESEND_API_KEY + RESEND_FROM (for outreach send; draft works without). Next: work the list (top lead Comfort-Tek, no website) -> outreach -> first clients. Free-tier note: org capped at 2 active Supabase projects, so used a schema in Guard Patrol rather than a new project.

Watch the remote-trust gap (M based in Lahore): use local phone/brand and close high-ticket in person on DTW trips. Read lawyer/dental ad + privacy rules before those niches.

**Jun 4 2026 — major session state (compaction checkpoint):**
- **GIT NOW EXISTS:** whole `Websites/` workspace backed up to private repo **`github.com/mfeerasta/feerstone`** (.gitignore excludes all .env/briefs/data/media; secrets in vault only). `site-template/` excluded (own repo `feerstone-demo` w/ CI); production site = `northbridge-digital`. Future changes need a manual commit+push (no CI on the main repo). Cloned production site still at `/tmp/feerstone-fixes`.
- **AI-MARKETING STRATEGY (M's directive: Feerstone = local AI + websites + marketing ONLY).** Plan at `~/.claude/plans/clever-splashing-narwhal.md`; cold-call tactics in `docs/cold-call-playbook.md`; pitch in `docs/comfort-tek-pitch.md` + `docs/comfort-tek-pitch-links.md`. Thesis: land with the website, expand to a **"Growth System" retainer ($697-997/mo): missed-call-text-back + reviews engine + local SEO + lead site + ROI report.** Lead pitch = speed-to-lead ("1 in 4 calls missed, 78% buy from first responder"). Killer wedge = free **database-reactivation** sprint. **CUSTOM BUILD chosen (NOT GoHighLevel).** Two parallel doors to pursue: **medspa white-label apps** + **white-label to marketing agencies (~$200/mo wholesale)**. CARVE-OUT to a different business: AI OS/"second brain", FDE enterprise consulting, data-scraping (don't correlate w/ local AI/web/marketing). Direct-to-trades is the HARDEST channel (research-confirmed) — survives only because demo-first.
- **GROWTH SYSTEM BUILT (custom, on our stack):** `growth` schema applied to Supabase `qcvxefbrzkspoldjydrx` (clients/jobs/missed_calls/reactivation, RLS service-role only, reached via direct Postgres `GROWTH_DATABASE_URL` in .env since growth isn't PostgREST-exposed). lead-engine modules: `clients.ts` (`--from-prospect`), `reviews.ts` (review-request + AI reply drafts), `dbr.ts` (reactivation), `report.ts` (ROI), `growth-db.ts` (shared Bun SQL). Comfort-Tek is the first `growth.clients` row. **`mctb-worker/`** = Cloudflare Worker (Twilio voice webhook: ring biz line → on no-answer text caller + log). Its 2 SECURITY DEFINER RPCs (`mctb-worker/migrations/0002-mctb-rpcs.sql`) NOT YET APPLIED (classifier blocked — needs explicit M approval). Worker NOT deployed yet.
- **THE BLOCKER for all SMS go-live = Twilio A2P 10DLC** (+ trial account can't text unverified numbers; error 30034/21608). Until then all SMS code runs dry-run/logs-only. Voice calls are NOT A2P-gated (work now).
- **Comfort-Tek pitch (tomorrow):** owner = **David Jones, goes by "Dave"**, sole proprietor since 2016, the decision-maker (no gatekeeper). Pitch packet (PDFs) in `~/Downloads/comfort-tek-pitch/`. Demos live: `comfort-tek.demo.feerstone.com` (Lovable site) + the personalized video on R2.
- New MCPs added this session: **Twilio** (`@twilio-alpha/mcp`), Remotion. Already had Ahrefs (SEO — covers most; add DataForSEO only when first SEO retainer signs). M PDF-for-pitches rule saved to global CLAUDE.md.

**LIVE state (Jun 3 2026) — overrides older deploy/infra notes above:**
- **feerstone.com on Cloudflare DNS** (zone `b9796be2fa2189c9c3567169e4753b53`, account `61d561ca9690ad9b59d9449181d90f12`, NS `newt`+`paityn.ns.cloudflare.com`). Migrated off Hostinger DNS; all email preserved (Hostinger MX mx1/mx2 + Resend amazonses + DKIM/SPF/DMARC mirrored as dns-only/grey-cloud). Email records must NEVER be touched.
- **Production site = Cloudflare Worker `feerstone`** (SSR), repo `mfeerasta/northbridge-digital` cloned at /tmp/feerstone-fixes (TanStack Start + React19 + Vite/Nitro + Tailwind4 + shadcn). **CI LIVE (Jun 3 2026): GitHub Actions auto build+deploy on push to main** (.github/workflows/deploy.yml — bun install/build → cloudflare/wrangler-action). Repo secrets CLOUDFLARE_API_TOKEN (Workers token, ~Jul 3 2026 expiry → re-set via `gh secret set` when it dies) + CLOUDFLARE_ACCOUNT_ID. Manual `bunx wrangler deploy` still works as fallback. wrangler.toml (committed): main dist/server/server.js, assets dist/client, nodejs_compat. Live local-SEO build-out: `/websites/locations/[15 cities WIN+DET]` + `/websites/services/[website-design,local-seo,google-business-profile,website-care]` + sitemap/robots/llms.txt. Form tables now in OWN Supabase `qcvxefbrzkspoldjydrx` public schema (audit_/briefing_submissions, analytics_events). Lead notifications via Resend (RESEND_API_KEY set as Worker secret).
- **Demo site = Worker `feerstone-demo` at https://demo.feerstone.com** — Astro static from site-template/dist, FICTIONAL "Maple Ridge Heating & Cooling" Windsor HVAC (clearly demo-labelled). This is the outreach link. Now its own repo `mfeerasta/feerstone-demo` (PRIVATE; site-template git-init'd + pushed) with the SAME GitHub Actions CI (push to main → build → wrangler-action deploy; same two repo secrets). site-template is also the reusable client-site factory template.
- **Outreach copy** (lead-engine/src/outreach.ts + archie-website-funnel/scripts/draft.py) now embeds demo.feerstone.com + greets "there" (vault has no contact-name field). Batch 1 = SMS scripts at outputs/outreach-batch1-sms.md (10 no-site prospects, phone channel — no emails on file).
- **GBP+reviews engine scaffolded:** lead-engine/src/reviews-engine.ts + gbp-audit.ts + docs/gbp-reviews-engine.md. Needs Google Business Profile API creds (manual Google approval, days) before live; logic ready behind stubs. Proposed leads.client_reviews table DDL not yet run.
- Cloudflare tokens in vault: `CLOUDFLARE_WORKERS_API_TOKEN` (deploy), `CLOUDFLARE_MIGRATION_API_TOKEN` (zone/DNS).

**Personalized-demo + video engine (Jun 3 2026):**
- **Demos built from prospect's REAL data** (M's directive — standard agency method, confirmed by research: Localo/Dorik auto-build from Google Business Profile; Firecrawl→AI-builder for existing sites). Our edge: official **Google Places API** (key `GOOGLE_MAPS_API_KEY`), NOT scraping → no captcha/IP-ban risk. Pipeline (lead-engine, bun): `bun run brief --place-id <id>` (src/client-brief.ts → Places Details rich fields: up to 5 real reviews, ~6 downloaded photos, hours, categories, NAP; + Firecrawl the existing site if any → `briefs/<slug>.json`) → `bun run config --place-id <id> [--url https://<slug>.demo.feerstone.com]` (src/brief-to-config.ts → writes `site-template/clients/<slug>/site.config.ts` + photos, maps businessType/services/real reviews/hours, never fabricates) → `CLIENT=<slug> bun run build` in site-template (per-client build via `CLIENT` env + `import.meta.glob` config selector; outDir `dist/<slug>`; default build still = Maple Ridge demo). No-website prospect = GBP-only path; weak-site = GBP + Firecrawl rebuild. Full playbook: `docs/demo-build-methodology.md`.
- **First real personalized demo LIVE: `comfort-tek.demo.feerstone.com`** (worker `feerstone-demo-comfort-tek`) — Comfort-Tek's real GBP data. Deploy pattern: build `dist/<slug>` → wrangler deploy a per-client worker → attach `<slug>.demo.feerstone.com` custom domain via migration token. Scalable alt (for many): R2 + wildcard `*.demo.feerstone.com` router worker.
- **Personalized outreach VIDEO engine:** `prospect-video/` (Remotion v4, npm, separate from bun lead-engine) composition `ProspectOutreach` (data-driven: name/niche/city/competitor/reviews + SERP screenshot + demo screenshot + ElevenLabs VO). Pipeline `bun run video --place-id <id>` (lead-engine/src/video-outreach.ts): vault→script→ElevenLabs VO (key wired in .env, reused Polymath ELEVENLABS key)→Playwright screenshots (manual: their Google SERP + demo)→Remotion render→mp4. First video hosted on **Cloudflare R2** bucket `feerstone-videos` (public, enabled Jun 3): `https://pub-2860cefa6dca4d65af0208f706dde100.r2.dev/<file>.mp4`. R2 free tier = 10GB (~1250 videos @8MB), reads/egress effectively unlimited.
- **Website-verification fix (Jun 3 2026) — CRITICAL methodology lesson:** Google Places `websiteUri` absence ≠ no website. Many businesses have a site NOT linked on their Google profile (e.g. Comfort-Tek has comforttekheatingandcooling.com; Places returned none → we mislabeled "no website"). Both lead-engine (`src/website-discovery.ts` shared module, used by client-brief.ts + `src/check-sites.ts`) AND Archie (hermes `common.py discover_website` — DEPLOYED + verified live, applies `leads.prospects.website_source` col via migration 0001) now DISCOVER the real domain via Firecrawl search when Places has none: search "<name> <city> official website", reject directories/socials/aggregators, score the **registrable domain (eTLD+1)** against the business-name slug (NOT the subdomain — else `comfort-tek...wheree.com` aggregator trap matches), accept ≥60 confidence else null (null over wrong guess — a wrong domain builds a demo from someone else's site). `website_source`: places|discovered|manual|none. Outreach copy (lead-engine outreach.ts + Archie draft.py) NEVER claims "no website" — leads with always-true ranking pain ("doesn't come up in Google's local results"); has-site → redesign angle, no-site → build angle. `bun run check-sites --batch1` audits a set. **Batch-1 result: 6 of 10 had sites Places didn't report** (Comfort-Tek, Clover cloverautorepair.com, DMA dma-electric.com, Alpina alpinacarcenter.com, Detroit Complete detroitautorepaircenter.com, Budget justcallbudget.com = REDESIGN; Vaid/Moe Joe's/Highland Park/Riviera = NEW BUILD, but Moe Joe's moesautocenters.com + Riviera reysauto.com were rejected close-calls worth manual check). Leadgen `--check-sites` flag verifies before labeling.
- **First client = Comfort-Tek (Windsor HVAC), built pro in Lovable (Jun 3 2026):** Lovable project "Comfort-Tek Windsor" id `aac7dc26-ddb6-4496-a0ac-aeefb534b642` (workspace `hEd5r04gshtMrNiMQoiO` "M's Lovable", TanStack/modern SSR), preview https://id-preview--aac7dc26-ddb6-4496-a0ac-aeefb534b642.lovable.app — real data (tagline "Bring Comfort to Your Life", 4.9★/65, Trane/Continental dealer, 8 services, 5 real reviews, financing), noindex (still a pitch until signed). M's preference: build the polished client site in Lovable (vs the Astro site-template demo which stays the fast outreach mockup).
- **Legal/compliance:** `docs/legal-compliance.md` (not legal advice). Scraping public data ~OK (we use official Places API). Top risk = public demos republishing Google reviews + customer photos. SAFE-DEFAULTS IMPLEMENTED: client demos now `noindex,nofollow,noarchive` + robots `Disallow: /` (private mockups; gated by build-time `CLIENT` env via `IS_CLIENT_DEMO`); customer-attributed GBP photos dropped (only business-owned kept — e.g. Comfort-Tek's "Roy C." photo excluded); demoBanner hardened to "Unofficial demo built by Feerstone — not affiliated/authorized"; no business logos. Outreach: CASL (Canada, implied consent via conspicuous publication + sender ID + opt-out), US cell texts manual-only (TCPA). Reviews kept attributed on the now-private demos.
- **Prospect video VO:** professional ElevenLabs read (reused Polymath ELEVENLABS key, M-approved), scene-ordered script + synced subtitle captions (`prospect-video/src/captions.ts` is the timing source of truth), real Google SERP + real demo shown. Note: TABL Anthropic key in vault is DEAD (401); Polymath Anthropic key works (used for brief LLM offerings extraction). Both reused per-M short-term; dedicated Feerstone keys preferred.
- Archie runs as user **hermes** (`/home/hermes/.hermes/`), NOT root — skill at `/home/hermes/.hermes/skills/website-funnel/`, runs under venv `/usr/local/lib/hermes-agent/venv/bin/python` (has asyncpg+firecrawl), keys FIRECRAWL_API_KEY+GOOGLE_MAPS_API_KEY+SUPABASE_DB_URL_RW present in `/home/hermes/.hermes/.env`. (DEPLOY.md in archie-website-funnel has plaintext keys — pre-existing, flagged to move to vault.)
- **MCPs:** Remotion (`claude mcp add remotion -- npx @remotion/mcp@latest`, added), Higgsfield (already connected via claude.ai — image/video gen + virality_predictor for ad creatives/b-roll). NOT yet connected (need M's keys/OAuth): Perplexity (`PERPLEXITY_API_KEY` → research hooks, src/research.ts stub ready), HeyGen (avatar, remote OAuth from HeyGen dashboard), Google Ads (read-only, OAuth+dev token; growth-tier product in `docs/google-ads-growth-tier.md`). Stub-and-document pattern (like gbp-audit) used for unwired capabilities. Full MCP guide: `docs/mcp-stack.md`.
